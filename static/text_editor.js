
/*
$(document).ready(function(){
	  
	  var text = tinymce.get("texteditor").getContent();
	  alert(text)
	  var ourRequest = new XMLHttpRequest();
	  ourRequest.open('POST','/update',true)
	  ourRequest.send(text);
	  });
*/
/*
function sendUpdate()
	  {
	  var textArea = document.getElementById('texteditor');
	  var text = textArea.value;
	  alert("hiiiiiiiiiiiiiiiiii")
	  var ourRequest = new XMLHttpRequest();
	  var params = text;
	  ourRequest.open('POST','/update',true)
	  ourRequest.send(params);
	  } */
/*	  
$.ajax({
  type: "POST",
  url: url,
  data: data,
  success: success,
  dataType: dataType
}); */
 /* $(document).ready(function(){
	$("#get-update").submit(function(e){
		vae content = tinymce.get("texteditor").getContent();
		
	});
});


function sendUpdate(content) {
	var ourRequest = new XMLHttpRequest();
	ourRequest.open('POST','/update',true)
	ourRequest.send(content);
}
*/